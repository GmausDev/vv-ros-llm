# Configuration

This directory contains configuration files for the V&V ROS LLM framework.

- `default.yaml` — Default configuration for all pipeline components (LLM providers, Docker, V&V pipeline, benchmarks, metrics, experiment settings).

Override any setting by copying `default.yaml` to a new file and passing it via the CLI.
